<div class="navbar navbar-fixed-top">
    <div class="navbar-inner">
        <div class="container-fluid">
            <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
                <span class="icon-bar"></span><span class="icon-bar"></span><span class="icon-bar"></span>
            </a><a class="brand" href="#">BLUG</a>
            <div class="nav-collapse">
                <ul class="nav">
                    <li class="@ViewBag.Title == "Home Page" ? "active">@Html.ActionLink("Home", "Index", "Home")</li>
                    <li class="@ViewBag.Title == "About" ? "active">@Html.ActionLink("About Us", "About", "Home")</li>
                    <li class="@ViewBag.Title == "History" ? "active">@Html.ActionLink("History", "History", "Home")</li>
                    <li class="@ViewBag.Title == "Contact Us" ? "active">@Html.ActionLink("Contact Us", "ContactUs", "Home")</li>
                </ul>
                @if (ViewBag.UserIsLoggedIn == "true")
                {
                    <p class="navbar-text pull-right">
                        Logged in as <a href="#">@ViewBag.UserName</a>
                    </p>
                }
            </div>
            <!--/.nav-collapse -->
        </div>
    </div>
</div>
